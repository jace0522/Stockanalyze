# PC가 꺼져 있어도 알림 받기 (GitHub Actions)

GitHub의 무료 서버가 정해진 시간에 스크립트를 대신 돌려줍니다.
내 PC는 꺼져 있어도 됩니다. 아래 3단계면 끝나요.

---

## 1단계 — GitHub에 비공개 저장소 만들기

1. https://github.com/new 접속
2. **Repository name**: `toss-news-alert` (아무 이름이나 OK)
3. **Private** 선택 ← 꼭 비공개로!
4. 나머지는 건드리지 말고 (README·gitignore 추가 X) **Create repository** 클릭
5. 다음 화면에 나오는 주소를 복사 (예: `https://github.com/내아이디/toss-news-alert.git`)

## 2단계 — 코드 올리기

이 폴더에서 아래 명령을 실행하세요. `내아이디` 부분만 본인 것으로 바꾸면 됩니다.

```bash
cd "C:\Users\jaehy\Desktop\주식분석\토스뉴스알리미"
git remote add origin https://github.com/내아이디/toss-news-alert.git
git push -u origin main
```

로그인 창이 뜨면 GitHub 계정으로 로그인하세요.
(비밀번호 대신 브라우저 인증이 뜨면 그대로 진행하면 됩니다)

## 3단계 — 비밀키 등록 (제일 중요)

API 키는 코드에 올라가지 않았습니다. GitHub의 안전한 금고에 직접 넣어주세요.

1. 저장소 페이지 → **Settings** 탭
2. 왼쪽 메뉴에서 **Secrets and variables** → **Actions**
3. **New repository secret** 버튼으로 아래 3개를 하나씩 추가:

| Name (이대로 정확히) | Secret (값) |
|---|---|
| `TELEGRAM_TOKEN` | 텔레그램 봇 토큰 (`8771...:AAH...`) |
| `TELEGRAM_CHAT_ID` | 채팅 ID (`8974584404`) |
| `ANTHROPIC_API_KEY` | Claude API 키 (`sk-ant-...`) |

값은 로컬 `config.json` 파일에 있는 것을 그대로 복사해서 붙여넣으면 됩니다.

---

## 잘 됐는지 확인하기

저장소 → **Actions** 탭 → 왼쪽에서 **아침 브리핑** 선택 →
오른쪽 **Run workflow** 버튼으로 지금 바로 한 번 실행해볼 수 있습니다.
1~2분 뒤 텔레그램에 브리핑이 오면 성공!

## 실행 일정

| 워크플로 | 실행 시각 (한국시간) | 하는 일 |
|---|---|---|
| 아침 브리핑 | 평일 오전 7시 | 뉴스 분석 + 브리핑 + 실적 알림 |
| 급등락 감시 | 장중 30분마다 | ±5% 급등락 시 즉시 알림 |
| 주간 성적표 | 일요일 오전 10시 | 주간 요약 + 적중률 복기 |

> GitHub Actions의 예약 실행은 서버가 붐빌 때 5~20분 정도 늦어질 수 있습니다. 정상입니다.

## 비용

비공개 저장소는 **월 2,000분 무료**입니다. 위 일정은 월 400~500분 정도 쓰므로
무료 범위 안입니다. (Claude API 사용료는 별도이며 기존과 동일)

## 대시보드는 어떻게 보나요?

클라우드가 분석한 결과(`feed.js`)는 저장소에 자동 저장됩니다.
최신 내용을 PC로 가져오려면:

```bash
cd "C:\Users\jaehy\Desktop\주식분석\토스뉴스알리미"
git pull
```

그 다음 `대시보드.html`을 열면 클라우드가 분석한 최신 결과가 보입니다.

## 설정을 바꾸고 싶을 때

종목이나 알림 기준을 바꾸려면 `config.json.example`을 수정하고 다시 올리세요.

```bash
git add config.json.example
git commit -m "종목 변경"
git push
```

(클라우드는 `config.json.example` + Secrets 조합으로 동작합니다.
로컬 실행은 기존처럼 `config.json`을 사용하니 둘 다 바꿔주면 됩니다)

## 로컬 실행은 계속 되나요?

네. `시작.bat`, `새로고침.bat` 모두 그대로 동작합니다.
윈도우 작업 스케줄러도 살아 있는데, 클라우드와 중복으로 알림이 오는 게 싫으면
작업 스케줄러에서 "토스뉴스알리미" 관련 작업 3개를 사용 안 함으로 두면 됩니다.
