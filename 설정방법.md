# 토스뉴스알리미 설정 방법

토스증권이 모아주는 종목 뉴스를 주기적으로 확인해서, AI(Claude)가 호재/악재를 판단하고
중요한 뉴스만 텔레그램으로 알려주는 앱입니다.

시작하려면 `config.json`에 3가지만 채우면 됩니다.

## 1. 텔레그램 봇 만들기 (1분)

1. 텔레그램에서 **@BotFather** 검색해서 대화 시작
2. `/newbot` 입력 → 봇 이름 아무거나 (예: 뉴스알리미) → 봇 아이디 입력 (예: `jaehy_news_bot`, 끝이 `bot`이어야 함)
3. BotFather가 주는 **토큰**(`123456789:AAH...` 형태)을 복사해서
   `config.json`의 `"텔레그램_토큰"`에 붙여넣기

## 2. 채팅 ID 알아내기

1. 방금 만든 내 봇을 텔레그램에서 검색해서 **아무 메시지나 하나 보내기** (예: "안녕")
2. 이 폴더에서 명령 프롬프트를 열고 실행:
   ```
   %LOCALAPPDATA%\Programs\Python\Python312\python.exe news_alert.py --get-chat-id
   ```
3. 나온 숫자를 `config.json`의 `"텔레그램_채팅ID"`에 붙여넣기

## 3. Claude API 키 만들기

1. https://console.anthropic.com 접속 → 가입/로그인
2. 결제 수단 등록 후 크레딧 충전 ($5면 몇 달 씀 — 뉴스 1건 분석에 약 1~2원)
3. **API Keys** 메뉴에서 키 생성 → `config.json`의 `"ANTHROPIC_API_KEY"`에 붙여넣기

## 4. 테스트 & 실행

```
:: 텔레그램 연결 테스트 (폰에 메시지 오면 성공)
%LOCALAPPDATA%\Programs\Python\Python312\python.exe news_alert.py --test

:: 실행 (창을 열어두면 계속 감시)
시작.bat 더블클릭
```

첫 실행 때는 기존 뉴스를 전부 "본 것"으로 처리하고(알림 폭탄 방지),
그 이후에 올라오는 **새 뉴스부터** 분석해서 알림을 보냅니다.

## 자동 실행 (작업 스케줄러 3개 등록됨)

| 작업 | 주기 | 하는 일 |
|---|---|---|
| 토스뉴스알리미 | 매일 06:00 (≈미국 장 마감 직후) | 뉴스 분석 + **아침 브리핑 1통** + 실적 발표 알림. 초중요 뉴스(중요도 4+)만 개별 알림 |
| 토스뉴스알리미 시세감시 | 30분마다 | 전일 대비 ±5% 이상 급등락 시 즉시 알림 (5%마다 단계별 재알림) |
| 토스뉴스알리미 주간리포트 | 일요일 10:00 | 주간 성적표: 베스트/워스트 종목, 뉴스 통계, 판정 적중률 복기 |

PC가 꺼져 있던 시간의 작업은 다음에 PC를 켤 때 보완 실행됩니다.

**실시간 감시 (선택)**: `시작.bat`을 더블클릭하면 창이 열려 있는 동안 10분마다 확인합니다.

## 시세가 오래됐을 때 🔄

대시보드의 시세는 마지막 실행 시점 기준입니다 (30분마다 시세감시가 자동 갱신).
지금 당장 최신 시세를 보고 싶으면 **`새로고침.bat` 더블클릭** — 2~3초 만에 시세를
갱신하고 대시보드를 자동으로 열어줍니다. 대시보드 상단에 데이터가 몇 분 전
것인지 표시되고, 40분 이상 오래되면 경고가 떠요.

## 판정 복기 (적중률) 🎯

호재/악재로 판정한 뉴스는 하루가 지나면 실제 주가로 자동 채점됩니다
(호재→다음 거래일 상승이면 적중). 대시보드 뉴스 카드에 건별 결과(✅/❌)가 붙고,
진단 보드 아래에 최근 30일 적중률이 표시됩니다. 주간 리포트에도 포함돼요.

## 대시보드 🎮

`index.html`을 더블클릭하면 픽셀 게임풍 상황실이 열립니다.
기본 화면은 **오늘의 종합 진단** — 종목별 종합 점수(100점 만점)와 🟢강세/⚪중립/🔴약세
신호, 한 줄 총평만 보여주는 간단 모드입니다. "상세 보기"를 누르면 진단 보드·뉴스 피드가 펼쳐집니다.

**점수 계산 방식** (매일 같은 공식으로 계산 — 참고용 의견입니다):
- 📰 뉴스팀 30%: 최근 3일 호재/악재 뉴스의 중요도 가중 합산
- 🦊 타로(기술) 30%: 추세 판정 + 20일 등락 + 52주 위치
- 🐰 디아나(재무) 25%: PER·PBR·배당 밸류에이션
- 🎧 바이브(민심) 15%: 커뮤니티 활성도 (과열은 역지표로 감점)
- 신호: 종합 65점 이상 🟢강세, 40점 이하 🔴약세
- 시세 전광판 (마지막 확인 시점 기준)
- 애널리스트 6인방: 불리(호재)·베어(악재)·부엉(중립 뉴스) + 타로(기술적 분석)·디아나(재무제표)·바이브(커뮤니티 민심)
- **종목 진단 보드**: 종목별 30일 미니차트 + 타로의 추세 진단(RSI·이동평균·52주 위치)
  + 디아나의 재무 진단(PER·PBR·배당, 튼튼/보통/주의 등급)
  + 바이브의 민심 진단(토스 커뮤니티 시간당 댓글 속도·실시간 인기 순위, 조용/보통/시끌/과열)
- 분석된 모든 뉴스 카드 (호재/악재/중립·종목별 필터, 적중 결과 ✅/❌, 최근 300건)

진단 데이터 출처: 차트·커뮤니티=토스증권, 재무지표=네이버증권, 실적 일정=나스닥. 매 확인 주기마다 갱신됩니다.

## 📱 폰에서 시키기 (파일 클릭 안 해도 됨)

**텔레그램 봇에게 말만 걸면 됩니다.** 최대 5분 안에 실행돼요.

| 보낼 말 | 하는 일 |
|---|---|
| `/새로고침` | 최신 시세·풍향계로 대시보드 갱신 |
| `/토론 NVDA` | 그 종목을 불리 vs 베어가 토론 (쉼표로 여러 개: `/토론 NVDA,TSLA`) |
| `/브리핑` | 지금 바로 아침 브리핑 받기 |
| `/종목` | 지금 감시 중인 종목 목록 |
| `/종목추가 PLTR` | 새 종목 넣기 (여러 개는 `PLTR,AMD`) |
| `/종목삭제 PLTR` | 빼기 |
| `/도움말` | 명령 목록 다시 보기 |

슬래시를 빼먹거나(`브리핑`) 슬래시 뒤에 띄어쓰기가 들어가도(`/ 브리핑`) 알아듣습니다.
같은 명령이 여러 번 쌓여 있으면 한 번만 실행해요.

### 감시 종목은 stocks.json 하나로 관리합니다

미국은 티커(`PLTR`), 한국은 6자리 코드(`005930`)를 씁니다. 최대 30개.

- **폰에서**: `/종목추가 PLTR` — 토스에 실제로 있는 종목인지 확인하고 넣습니다
- **PC에서**: `stocks.json` 을 직접 고쳐도 됩니다

> 예전에는 `config.json`(내 PC)과 `config.json.example`(클라우드) 두 군데에
> 똑같이 적어야 했는데, 이제 `stocks.json` 한 곳만 고치면 양쪽에 반영됩니다.
> 새로 넣은 종목의 **기존 뉴스는 전부 "본 것"으로 처리**하니 알림이 쏟아지지 않아요.

깃허브 Actions가 5분마다 새 메시지를 확인해서 처리합니다(`commands.yml`).
**바로 실행하고 싶으면** Actions 탭 → "직접 실행 (새로고침 · 토론)" → Run workflow.

> 보안: **내 채팅에서 온 메시지만** 처리합니다. 봇 주소를 아는 다른 사람이
> 말을 걸어도 무시해요. 종목 이름도 영문 1~5자 / 숫자 6자리 형태만 통과시킵니다.
>
> 대시보드 웹페이지에 직접 실행 버튼을 달지 않은 이유: 공개 페이지에 깃허브
> 토큰을 넣어야 하는데, 그러면 누구나 그 토큰으로 저장소를 건드릴 수 있어요.

## 🥊 원하는 종목 토론시키기

**`토론시키기.bat` 더블클릭** → 종목 입력 → 끝. (여러 개는 쉼표로: `NVDA,TSLA`)

명령으로 직접 하려면:

```bash
%LOCALAPPDATA%\Programs\Python\Python312\python.exe news_alert.py --debate NVDA
```

불리(낙관)와 베어(비관)가 3라운드 주고받고 부엉이가 판정합니다.
결과는 텔레그램으로 오고, 대시보드를 새로고침하면 토론 카드가 보입니다.
종목당 20초 정도, 약 12원이 듭니다.

> 아침 브리핑 때는 의견이 갈리는 종목 2개를 알아서 골라 토론합니다.

## 💰 적정 수량 계산 (ATR 기반)

"한 번 매매에 계좌의 1%만 잃는다"는 원칙으로 살 수량을 역산해줍니다.

```
위험예산 = 계좌금액 × 거래당_위험퍼센트
주문수량 = 위험예산 ÷ (ATR × 손절_ATR배수)
```

**수량을 먼저 정하고 손절을 끼워 맞추면 한도가 깨지므로, 손절폭이 수량을 정합니다.**
변동성이 큰 종목(SOXL)은 자동으로 적게, 조용한 종목은 많이 잡혀서 감수하는 손실은 일정해집니다.
종목당 최대 비중(기본 20%)을 넘으면 한 번 더 줄이고 ⚠상한으로 표시됩니다.

진단 보드의 "💰 적정 수량" 열에서 수량·투입금·비중·손절가·최대손실을 볼 수 있습니다.


## config.json 새 설정

| 항목 | 설명 |
|---|---|
| 즉시알림_최소중요도 | 아침 브리핑 모드에서 개별 알림을 보낼 기준 (기본 4 — 나머지는 브리핑에 요약) |
| 급등락_기준퍼센트 | 급등락 알림 기준 (기본 5%) |
| 토론_사용 / 토론_종목수 | 불리 vs 베어 토론 (기본 켬 / 2종목, 종목당 약 12원) |
| 계좌금액 | 적정 수량 계산의 기준 금액 (원화). 이 값은 내 PC의 config.json에만 있고 공개되지 않습니다 |
| 거래당_위험퍼센트 | 한 번 매매에 감수할 계좌 대비 손실 (기본 1%) |
| 손절_ATR배수 | 손절폭 = ATR × 이 값 (기본 2) |
| 종목당_최대비중 | 한 종목에 넣을 수 있는 최대 비중 (기본 20%) |

## config.json 설정 항목

| 항목 | 설명 |
|---|---|
| 종목 | 감시할 종목. 미국은 티커(`TSLA`), 한국은 종목코드(`005930`=삼성전자) |
| 확인주기_분 | 실시간 감시 모드에서 몇 분마다 확인할지 (기본 10분) |
| 최소중요도 | 이 값(1~5) 이상인 뉴스만 알림 (기본 3 — 낮추면 알림이 많아짐) |
| 중립알림 | `true`면 중립 뉴스도 알림 (기본 false — 호재/악재만) |
| 뉴스조회_개수 | 종목당 한 번에 가져올 뉴스 수 (기본 30) |
| 사이클당_최대분석 | 종목당 한 번에 분석할 최대 뉴스 수 (기본 20 — API 비용 안전장치) |
| AI모델 | 분석에 쓸 모델 (기본 Haiku — 싸고 빠름) |

## 참고

- 분석 기록은 `news_log.txt`에 쌓입니다. 어떤 뉴스가 무슨 판정을 받았는지 볼 수 있어요.
- 이 앱의 판단은 AI의 의견일 뿐, 투자 판단의 근거로 삼기엔 부족할 수 있습니다.
